name: Deploy Tierney's concept to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  staging:
    name: Deployment update in progress
    runs-on: ubuntu-latest
    steps:
      - run: echo "The real-photo GitHub Pages implementation is being staged."
